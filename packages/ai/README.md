# @gameenginejs/ai

Thin Node service that will orchestrate OpenAI + Claude calls.
- Accepts prompt payloads.
- Dispatches to the right model (future work).
- Returns project graph deltas + summaries for the editor UI.
